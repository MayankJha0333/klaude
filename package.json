{
  "name": "iridescent",
  "displayName": "Iridescent",
  "description": "Agentic AI coding assistant for VS Code — powered by your Claude Code subscription.",
  "version": "0.1.0",
  "publisher": "iridescent",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "iridescent",
          "title": "Iridescent",
          "icon": "$(sparkle)"
        }
      ]
    },
    "views": {
      "iridescent": [
        {
          "type": "webview",
          "id": "iridescent.chat",
          "name": "Chat",
          "contextualTitle": "Iridescent"
        }
      ]
    },
    "commands": [
      {
        "command": "iridescent.newChat",
        "title": "Iridescent: New Chat"
      },
      {
        "command": "iridescent.toggleChat",
        "title": "Iridescent: Toggle Chat Panel"
      },
      {
        "command": "iridescent.cycleMode",
        "title": "Iridescent: Cycle Permission Mode"
      },
      {
        "command": "iridescent.sendSelection",
        "title": "Iridescent: Send Selection to Chat"
      },
      {
        "command": "iridescent.commentOnSelection",
        "title": "Iridescent: Comment on Selection"
      },
      {
        "command": "iridescent.generateConventions",
        "title": "Iridescent: Generate CLAUDE.md"
      }
    ],
    "keybindings": [
      {
        "command": "iridescent.toggleChat",
        "key": "ctrl+shift+i",
        "mac": "cmd+shift+i"
      },
      {
        "command": "iridescent.cycleMode",
        "key": "shift+tab",
        "when": "iridescent.chatFocused"
      },
      {
        "command": "iridescent.sendSelection",
        "key": "ctrl+l",
        "mac": "cmd+l",
        "when": "editorHasSelection"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "iridescent.commentOnSelection",
          "group": "iridescent@1",
          "when": "editorHasSelection"
        }
      ]
    },
    "configuration": {
      "title": "Iridescent",
      "properties": {
        "iridescent.model": {
          "type": "string",
          "default": "claude-sonnet-4-5",
          "description": "Model ID passed to 'claude -p --model'. Use a CLI alias (sonnet, opus, haiku, opusplan, default) or an explicit version (claude-sonnet-4-6, etc.)."
        },
        "iridescent.permissionMode": {
          "type": "string",
          "enum": [
            "default",
            "plan",
            "auto"
          ],
          "default": "default",
          "description": "Tool approval behavior. 'default' prompts before each action; 'plan' is read-only / no execution; 'auto' auto-approves the Claude CLI's standard read/search tools and bash commands matching allowedBashPatterns."
        },
        "iridescent.maxTokens": {
          "type": "number",
          "default": 4096,
          "description": "Max output tokens per assistant turn. Lower = fewer rate-limit hits on subscription tokens."
        },
        "iridescent.allowedBashPatterns": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "^git (status|diff|log|branch)$",
            "^npm (test|run test)$"
          ],
          "description": "Regex patterns of bash commands auto-approved in auto mode."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.config.mjs && npm --prefix webview run build",
    "watch": "node esbuild.config.mjs --watch",
    "package": "vsce package",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.1.119"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
